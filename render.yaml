services:
  - type: web
    name: saint-augustine-ai-backend
    runtime: node
    plan: free
    rootDir: backend
    branch: main
    buildCommand: "npm install"
    startCommand: "npm start"
    envVars:
      - key: OPENROUTER_API_KEY
        sync: false
      - key: PORT
        value: 3000
