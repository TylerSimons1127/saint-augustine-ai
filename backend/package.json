{
  "name": "saint-augustine-ai-backend",
  "version": "1.0.0",
  "description": "Backend proxy for SaintAugustineAI (zero-dependency Node HTTP server).",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "test": "node _timeout_test.js && node _proxy_mirror_test.js && node _fallback_test.js && node _ratelimit_test.js && node _allowlist_test.js && node _metrics_test.js && node _feedback_test.js && node _contract_test.js"
  },
  "engines": {
    "node": ">=18"
  }
}
